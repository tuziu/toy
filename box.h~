#ifndef BOX_H
#define BOX_H

#include "uklad.h"
#include <cstddef>

class Box
{
private:
    size_t m_num_of_it;
    UkladEwol m_u;    

public:
    Box(size_2t u_size,size_t nu_i, double p=0.5);

private:
    Box();
    Box(const Box&);
    Box& operator = (const Box& );

public:
    std::vector<Result> grind();

};

#endif
